# Integrations

Implemented adapter foundations: Gmail REST, Microsoft Graph REST, and SMTP. PostgreSQL and Redis have live probes. Provider credentials, Supabase Auth, provider webhook signatures, AI model credentials, and production monitoring are `[TODO]` until real accounts/secrets are supplied.

Evidence: `apps/api/src/providers/`, `apps/api/src/routes/health.mjs`.
